

package Class

object TempTest {
  def main(args: Array[String]): Unit = {
		  def equal(other: Any) ={
				  val that = other.isInstanceOf[Int]
						  if(that == null) false
						  else true
		  }
    val i = 5
    println(equal(i))
  }
}