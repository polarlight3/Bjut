package Spark
import scala.collection.mutable._

object Mapp {
  def main(args: Array[String]): Unit = {
    val sc:Map[String, Int] = Map("Alice" -> 10, "Bob" -> 3, "Cindy" -> 8)
    var sc2 = Map(("Alice", 10), ("Bob", 3), ("Cindy", 8))
    for(a <- sc)   println(a)
    println()
    for(a <- sc2)   println(a)
  }
}