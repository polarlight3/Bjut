package Class


object Extends extends Enumeration{
  def doWhat(a:Value){
    if(a == Red) {println("red")}
    else if(a == Yellow) {println("yellow")}
    else {println("green")}
  }
  
  def main(args: Array[String]): Unit = {
		  val Red, Yellow, Green = Value
				 
		  println(Red)
    
  }
}